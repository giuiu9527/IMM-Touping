# 第三方引擎（体积大，不入库；发布包里另附）
scrcpy/
scrcpy.zip

# 运行时生成
records/
settings.json
devices.json
debug.log
app.log

# Python
__pycache__/
*.pyc

# PyInstaller 打包产物
build/
dist/
# 手改过的打包配置(exe名/图标/打包数据)要入库，其余 spec 忽略
*.spec
!IMM-Touping.spec
